{
    "name": "root",
    "version": "1.0.0",
    "description": "",
    "main": "server.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start:server": "cd server && npm install && npm start",
        "start:external_server": "cd external_server && npm install && npm start",
        "start:client": "cd client && npm install && npm start",
        "start": "npm-run-all --parallel start:*",
        "build:server": "cd server && npm install",
        "build:external_server": "cd external_server && npm install",
        "build:client": "cd client && npm install",
        "build": "npm-run-all build:*"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
